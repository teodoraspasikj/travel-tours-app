apiVersion: v1
kind: Service
metadata:
  name: travel-backend
  namespace: travel-tours
  labels:
    app: travel-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: travel-backend